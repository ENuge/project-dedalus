// @flow

// import axios from 'axios';

// This will be a GET and a POST
// POST: Accepts the latest day's worth of Anki updates,
// from a cronjob-esque thing that reads from my sqlite folder
// locally on my laptop (and has some sort of authentication
// builtin that's hidden from github...).
// This will require some sort of persistent storage, whether that's
// another GCP product or a quick sqlite instance.
// GET: Feeds that information over to the website in a nice-looking
// way.

// export const getAnki = (req, res) => res.send('TODO');

// export const postAnki = (req, res) => res.send('TODO');
